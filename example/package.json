{
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next export",
    "start": "next start",
    "raz": "rm -rf node_modules && rm -rf out_lambda && rm -rf .next && rm -rf out && rm -rf .aws-sam && rm -rf samconfig.toml"
  },
  "dependencies": {
    "next": "^10.0.5",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "next-aws-lambda-webpack-plugin": "file:../"
  }
}
